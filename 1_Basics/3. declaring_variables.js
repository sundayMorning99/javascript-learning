//const variable must be initialized - 'fixed' in Java
let codeFactory = 'These guys are easy and free';

//red underline sends a warning that this can't be processed. 
const errorMsg;

/**when a variable is declared, you can think of the variable as physical memory location.
 * we don't want the memory location with important information to be interfered or deleted.
 * This is why memory optimization - release unnecessary memory space and reused by other programs- is important. 
 */
